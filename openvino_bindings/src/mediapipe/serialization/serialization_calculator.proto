syntax = "proto2";
package mediapipe;

import "mediapipe/framework/calculator.proto";

message SerializationCalculatorOptions {
  extend mediapipe.CalculatorOptions {
      optional SerializationCalculatorOptions ext = 125115992;
  }
  optional bool overlay = 1;
  optional bool json = 2;
  optional bool csv = 3;
}
